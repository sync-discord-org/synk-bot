# synk-bot

A bot for discord using JS.

## Installation

1. Install Node.js (20+)
2. Run `npm init -y`
3. Run `npm install discord.js dotenv`
4. Create a `.env` file in root folder
5. Add your bot token to the `.env DISCORD_TOKEN=` file
6. Run `node index.js`

